export enum UserRole {
  'Super Admin' = 1,
  'Company Owner' = 2,
  'Dispatcher' = 3,
  'Driver' = 4,
}

export enum TripStatus {
  Planned = 1,
  InProgress = 2,
  Completed = 3,
  Cancelled = 4,
}
