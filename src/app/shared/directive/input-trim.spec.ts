import { InputTrim } from './input-trim';

describe('InputTrim', () => {
  it('should create an instance', () => {
    const directive = new InputTrim();
    expect(directive).toBeTruthy();
  });
});
